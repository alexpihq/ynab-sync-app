# Tron Blockchain Integration Setup

## Обзор

Интеграция с Tron blockchain позволяет автоматически импортировать USDT транзакции из вашего Tron кошелька в YNAB бюджет Innerly. Синхронизация происходит в одну сторону: **Tron Blockchain → YNAB Innerly**.

## Архитектура

### Tron Wallet

- **Wallet Address**: `TU3ZFzu6VedX4AydFtbfCTDKcJvKPBbtGp`
- **Token**: USDT (TRC-20)
- **Directions**: 
  - Incoming (to wallet) - положительные суммы в YNAB
  - Outgoing (from wallet) - отрицательные суммы в YNAB

### YNAB Account

- **Budget**: Innerly (`6dd20115-3f86-44d8-9dfa-911c699034dc`)
- **Account**: Tron USDT (`6f1039a4-ea55-46f6-a833-74f6c067a1b3`)
- **Currency**: USD (без конвертации, т.к. USDT ≈ USD)

### Tron API

- **API**: Tronscan API (`https://apilist.tronscanapi.com`)
- **Endpoint**: `/api/token_trc20/transfers`
- **Authentication**: TRON-PRO-API-KEY
- **Rate Limiting**: 1 RPS (запрос в секунду)
- **Retry**: При 429 ошибке ждем 30 секунд и повторяем

## Настройка

### 1. Переменные окружения

Добавьте в `.env`:

```env
TRON_WALLET_ADDRESS=TU3ZFzu6VedX4AydFtbfCTDKcJvKPBbtGp
TRON_API_KEY=87da2a1e-0651-4848-a91a-00b95660fd3a
```

### 2. Таблица маппинга

Таблица `tron_transaction_mappings` автоматически создается миграцией и содержит:

- `tron_wallet_address` - Адрес кошелька
- `tron_transaction_id` - ID транзакции в блокчейне (64-символьный hex)
- `tron_block_ts` - Unix timestamp (milliseconds)
- `tron_direction` - Направление ('in' или 'out')
- `tron_from_address` - Адрес отправителя
- `tron_to_address` - Адрес получателя
- `tron_amount` - Сумма в USDT (уже разделена на 1e6)
- `ynab_budget_id` - ID бюджета в YNAB (Innerly)
- `ynab_account_id` - ID счета в YNAB
- `ynab_transaction_id` - ID транзакции в YNAB
- `sync_status` - Статус синхронизации ('active', 'deleted')

## Логика синхронизации

### Получение транзакций

1. Отдельно запрашиваем входящие (`toAddress=wallet`) и исходящие (`fromAddress=wallet`) транзакции
2. Фильтруем только USDT токены (игнорируем другие TRC-20 токены)
3. Применяем фильтр по дате: только транзакции начиная с `SYNC_START_DATE` (2026-01-01)
4. Соблюдаем rate limit: минимум 1.2 секунды между запросами

### Создание транзакций

Для каждой Tron транзакции:
1. Определяем направление (in/out) по адресам
2. Конвертируем сумму: `quant / 1e6 * 1000` (в YNAB milliunits)
3. Для исходящих делаем сумму отрицательной
4. Создаем транзакцию в YNAB с:
   - `import_id` = `TRON:{first_28_chars_of_txid}`
   - `payee_name` = сокращенный адрес контрагента (TU3ZFz...tGp)
   - `memo` = "USDT from/to {address} | Tron"
5. Сохраняем маппинг в Supabase

### Обновление транзакций

Если транзакция существует в маппинге, проверяем изменения:
- Сумма (`tron_amount`)
- Timestamp (`tron_block_ts`)

При обнаружении изменений обновляем транзакцию в YNAB и маппинг.

**Примечание**: В blockchain транзакции immutable (неизменяемые), поэтому обновления крайне редки.

### Удаление транзакций

Blockchain транзакции не удаляются, но мы отслеживаем маппинги:
- Если транзакция исчезла из результатов API (например, из-за фильтрации)
- Удаляем соответствующую транзакцию в YNAB
- Обновляем статус маппинга на 'deleted'

### Конвертация сумм

**Tron → YNAB:**
- Tron использует `quant` для USDT: `1000000` = `1.00 USDT`
- YNAB использует milliunits: `1000` = `$1.00`
- Формула: `ynab_milliunits = (quant / 1e6) * 1000`

**Направление:**
- Incoming (получение): положительная сумма
- Outgoing (отправка): отрицательная сумма

### Обработка Payee

- Payee формируется из адреса контрагента (отправитель для входящих, получатель для исходящих)
- Адрес сокращается: `TU3ZFzu6VedX4AydFtbfCTDKcJvKPBbtGp` → `TU3ZFz...tGp`
- Memo: `USDT from TU3ZFz...tGp | Tron` или `USDT to TU3ZFz...tGp | Tron`

## Идемпотентность

Используем стабильный `import_id`:
```
TRON:{first_28_chars_of_transaction_id}
```

Пример:
- TX ID: `33d7c718c9ac7e9f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b`
- Import ID: `TRON:33d7c718c9ac7e9f1a2b3c4d`

При конфликте (409) добавляем timestamp (7 цифр):
```
TRON:33d7c718c9ac7e9f1a2b3c:1234567
```

## Фильтрация по дате

Синхронизируются только транзакции начиная с `SYNC_START_DATE` (по умолчанию `2026-01-01`).

## Rate Limiting

Tron API имеет ограничение 1 RPS (запрос в секунду):
- Минимальная задержка между запросами: 1.2 секунды
- При получении 429 ошибки: ждем 30 секунд и повторяем
- Максимум 3 попытки для каждого запроса

## Запуск синхронизации

### Одноразовая синхронизация
```bash
npm run sync-once
```

### Непрерывная синхронизация
```bash
npm start
```

## Структура кода

- `src/clients/tron.ts` - Клиент для Tron blockchain API
- `src/services/tronSync.ts` - Логика синхронизации Tron → YNAB
- `src/clients/supabase.ts` - Методы для работы с `tron_transaction_mappings`

## Логирование

Примеры логов:

```
⛓️  Starting Tron → YNAB Innerly synchronization...
   Fetching incoming USDT transactions...
   Fetching outgoing USDT transactions...
   Total USDT transactions: 15 (7 in, 8 out)
   
   💰 Creating YNAB transaction: 33d7c718c9ac7e9f... (out) - -10000.00 USD
   ✅ Created YNAB transaction for Tron TX 33d7c718c9ac7e9f...
   
   💰 Creating YNAB transaction: a1b2c3d4e5f6a7b8... (in) - 5000.00 USD
   ✅ Created YNAB transaction for Tron TX a1b2c3d4e5f6a7b8...

✅ Tron sync completed!
   Created: 12
   Updated: 2
   Deleted: 0
   Skipped: 1
```

## Troubleshooting

### Ошибка: "Tron not configured"
- Проверьте, что `TRON_WALLET_ADDRESS` и `TRON_API_KEY` установлены в `.env`

### Ошибка: "Tron API error: 429"
- Это rate limit - приложение автоматически ждет 30 секунд и повторяет
- Если ошибка повторяется, возможно превышен дневной лимит API

### Ошибка: "Import ID conflict (409)"
- Нормальная ситуация при попытке создать дубликат
- Система автоматически добавляет timestamp и повторяет попытку

### Медленная загрузка транзакций
- Это нормально из-за rate limiting (1.2 секунды между запросами)
- Для 100 транзакций потребуется ~2 минуты
- При большом количестве транзакций первая синхронизация может занять время

### Ошибка: "Only USDT tokens"
- Приложение фильтрует только USDT транзакции
- Другие TRC-20 токены игнорируются автоматически
- Это ожидаемое поведение

## Ограничения

1. **Только USDT**: Поддерживаются только USDT (TRC-20) транзакции
2. **Только односторонняя синхронизация**: Tron → YNAB
3. **Дата начала**: Только транзакции с `2026-01-01` и позже
4. **Rate Limiting**: 1 запрос в секунду, может быть медленным для большого объема данных
5. **API Key**: Требуется TRON-PRO-API-KEY для доступа к Tronscan API

## Безопасность

- API Key хранится в `.env` и не должен попадать в репозиторий
- Wallet address публичный - только для чтения транзакций
- Никаких приватных ключей не требуется
- Синхронизация только read-only из blockchain

## Будущие улучшения

- [ ] Поддержка других TRC-20 токенов (USDC, TUSD и т.д.)
- [ ] Batch processing для ускорения первоначальной загрузки
- [ ] Webhook notifications для новых транзакций
- [ ] Dashboard для мониторинга blockchain активности
- [ ] Поддержка нескольких кошельков





















