# TBank Business API Setup Guide

Этот гайд поможет настроить синхронизацию транзакций из ТБанка (Tinkoff Business) в YNAB.

## Особенности

- ✅ Автоматическая синхронизация операций по счетам ТБанка
- ✅ Поддержка рублевых, долларовых и фунтовых счетов
- ✅ Автоматическая конвертация RUB → USD через курсы в Supabase
- ✅ Распознавание получателей/отправителей для YNAB payees
- ✅ Детальная информация о карточных операциях (MCC, merchant, карта)
- ✅ Обработка как дебетовых, так и кредитовых операций

## Шаг 1: Получение API токена ТБанка

1. Войдите в личный кабинет [TBank Business](https://business.tbank.ru/)
2. Перейдите в раздел **API & Интеграции** → **API токены**
3. Создайте новый токен:
   - Название: `YNAB Sync`
   - Права доступа:
     - ✅ Информация о счетах компании
     - ✅ Информация об операциях компании
   - **Важно:** При создании токена укажите ваш IPv4 адрес
4. Скопируйте токен (он показывается только один раз!)

### Как узнать свой IP адрес

```bash
curl -4 ifconfig.me
```

Укажите этот IP адрес при создании токена ТБанка.

## Шаг 2: Настройка .env файла

Добавьте токен в `.env` файл:

```bash
# TBank Business API
TBANK_TOKEN=your_tbank_api_token_here
```

## Шаг 3: Создание таблицы в Supabase

Запустите SQL скрипт для создания таблицы маппинга:

```bash
# В Supabase SQL Editor выполните скрипт:
cat create_tbank_table.sql
```

Или через UI Supabase:
1. Откройте **SQL Editor** в вашем проекте Supabase
2. Скопируйте содержимое файла `create_tbank_table.sql`
3. Выполните скрипт

## Шаг 4: Настройка счетов и бюджета

Отредактируйте `src/services/tbankSync.ts`:

### 4.1. Получите список ваших счетов ТБанка

```bash
npm run test-tbank
```

Вы увидите список всех ваших счетов с их номерами.

### 4.2. Найдите ID аккаунта в YNAB

1. Откройте YNAB
2. Перейдите в нужный бюджет
3. Выберите аккаунт для синхронизации
4. ID аккаунта можно получить из URL или через YNAB API

### 4.3. Настройте маппинг в коде

В файле `src/services/tbankSync.ts` обновите константы:

```typescript
const TBANK_ACCOUNTS = {
  RUB: {
    tbankAccountNumber: '40802810300000990185', // Ваш номер счета из шага 4.1
    ynabAccountId: 'd92d652a-a66b-4081-93ff-f8b46fe49248', // YNAB account ID
    currency: 'RUB' as const,
  },
};

const YNAB_BUDGET_ID = '9c2dd1ba-36c2-4cb9-9428-6882160a155a'; // Ваш budget ID
```

## Шаг 5: Убедитесь, что курсы валют настроены

Для конвертации RUB → USD нужны курсы в Supabase:
- EUR/USD (`exchange_rates` таблица)
- EUR/RUB (`exchange_rates` таблица с полем `eur_to_rub`)

Конвертация происходит по цепочке: **RUB → EUR → USD**

Проверьте наличие курсов:

```sql
SELECT * FROM exchange_rates 
WHERE month >= '2026-01' 
ORDER BY month DESC 
LIMIT 5;
```

Если курсов нет, добавьте их через скрипт `update_exchange_rates.sql` или вручную.

## Шаг 6: Тестирование

### 6.1. Тест подключения к API

```bash
npm run test-tbank
```

Этот скрипт покажет:
- ✅ Все ваши счета ТБанка
- ✅ Балансы по счетам
- ✅ Последние операции
- ✅ Примеры конвертации для YNAB

### 6.2. Тестовая синхронизация (TODO)

```bash
# Создайте тестовый скрипт для синхронизации одной операции
npm run test-tbank-sync
```

## Шаг 7: Добавление в основной sync

Отредактируйте `src/services/sync.ts` и добавьте синхронизацию TBank:

```typescript
import { syncTbankToYnab } from './tbankSync.js';

// В функции performSync() добавьте:
if (tbank.isConfigured()) {
  logger.info('\n🏦 Syncing TBank → YNAB...');
  const tbankStats = await syncTbankToYnab();
  // ... обработка результатов
}
```

## Как это работает

### 1. Получение операций

- Скрипт запрашивает операции из TBank API за период с `SYNC_START_DATE` до текущей даты
- Используется статус `Transaction` (только подтвержденные операции)
- Поддерживается пагинация для больших выписок

### 2. Конвертация валюты

Для рублевых счетов:
```
8990 RUB → 8,990,000 milliunits (TBank)
→ EUR (по курсу из Supabase)
→ USD (по курсу из Supabase)
→ 90.23 USD (пример)
```

### 3. Маппинг данных

- **Дата**: берется из `operationDate`
- **Сумма**: 
  - Дебет (списание) = отрицательная
  - Кредит (зачисление) = положительная
- **Payee**: извлекается из merchant name или counterparty
- **Memo**: включает описание, merchant, номер карты

### 4. Дедупликация

Используется `import_id` в формате: `TBANK:{operation_id}`

YNAB автоматически предотвращает дублирование по этому ID.

### 5. Синхронизация

- Создание новых транзакций
- Обновление существующих (если изменились)
- Пометка удаленных (опционально)

## Структура данных TBank

### Операция (TbankOperation)

Основные поля из API:
```typescript
{
  operationId: string;        // UUID операции
  operationDate: string;      // ISO 8601 дата
  operationStatus: string;    // "Transaction" | "Authorization"
  typeOfOperation: string;    // "Debit" | "Credit"
  category: string;           // "cardOperation", "paymentOrder", etc.
  accountAmount: number;      // Сумма в валюте счета
  accountCurrencyDigitalCode: string; // "643" (RUB), "840" (USD)
  description: string;        // Описание операции
  counterParty?: {            // Контрагент
    name: string;
    inn?: string;
  };
  merch?: {                   // Для карточных операций
    name: string;
    city: string;
    country: string;
  };
  cardNumber?: string;        // Маскированный номер карты
  mcc?: string;               // MCC код
}
```

## Ограничения API ТБанка

- **RPS**: 20 запросов в секунду
- **Лимит на операцию**: 1000-5000 операций за запрос
- **История**: данные доступны с июня 2023 года
- **IP привязка**: токен работает только с указанных IP адресов

## Troubleshooting

### Ошибка: "FORBIDDEN - IP-адрес не указан"

**Решение**: Перевыпустите токен и укажите ваш IPv4 адрес (узнать: `curl -4 ifconfig.me`)

### Ошибка: "Exchange rate not found"

**Решение**: Убедитесь, что в таблице `exchange_rates` есть курсы для нужного месяца

### Транзакции не создаются

1. Проверьте, что `YNAB_BUDGET_ID` и `ynabAccountId` правильные
2. Проверьте логи: `npm run test-tbank-sync`
3. Убедитесь, что YNAB токен имеет права на запись

### Дублирование транзакций

Если YNAB создает дубликаты, проверьте:
1. Уникальность `import_id` (должен быть `TBANK:{operation_id}`)
2. Что в Supabase нет дублей в таблице `tbank_transaction_mappings`

## Полезные команды

```bash
# Тест подключения
npm run test-tbank

# Проверка счетов
curl -H "Authorization: Bearer $TBANK_TOKEN" \
  https://business.tbank.ru/openapi/api/v4/bank-accounts | jq

# Получение операций за период
curl -H "Authorization: Bearer $TBANK_TOKEN" \
  "https://business.tbank.ru/openapi/api/v1/statement?accountNumber=40802810300000990185&from=2026-01-01T00:00:00Z&to=2026-01-09T00:00:00Z&limit=10" | jq
```

## Дополнительная информация

- [Документация TBank API](https://business.tbank.ru/openapi/)
- [YNAB API Documentation](https://api.ynab.com/)
- Исходный код: `src/clients/tbank.ts` и `src/services/tbankSync.ts`

