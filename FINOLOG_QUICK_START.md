# Finolog Quick Start Guide 🚀

## Быстрая настройка (5 минут)

### Шаг 1: Проверьте .env файл

Убедитесь, что в `.env` добавлен токен Finolog:

```bash
FINOLOG_API_TOKEN=ваш_токен_здесь
```

⚠️ **Важно**: Название именно `FINOLOG_API_TOKEN`, а не `Finolog_key`!

### Шаг 2: Проверьте курс RUB

Курс EUR/RUB уже добавлен в базу данных:
- **Январь 2026**: 1 EUR = 94.36 RUB

Если нужны другие месяцы:
```sql
UPDATE exchange_rates 
SET eur_to_rub = 94.36 
WHERE month = '2026-02';
```

### Шаг 3: Конфигурация готова! ✅

Три аккаунта Finolog настроены автоматически:
- **161752** (RUB) → YNAB Account for Epic Web3
- **168187** (EUR) → YNAB Account for Epic Web3
- **161666** (USD) → YNAB Account for Epic Web3

Все транзакции конвертируются в EUR и сохраняются в личный бюджет Alex.

### Шаг 4: Запустите тест

```bash
npm run sync-once
```

### Что произойдет:

1. **YNAB ↔ YNAB синхронизация** (Innerly, Vibecon)
2. **Finolog → YNAB синхронизация** (Epic Web3):
   - Загрузятся транзакции из трех Finolog аккаунтов
   - Конвертируются в EUR по курсу
   - Создадутся в YNAB с memo "Description | Finolog Sync"
   - Import ID: `FINOLOG:accountId:transactionId`

### Логи, которые вы увидите:

```
🔄 Starting Finolog sync: Account 161752 (RUB) -> YNAB 5f0d008f-... (EUR)
📥 Found 5 Finolog transactions
📝 Creating YNAB transaction: 12345 (expense) - -100.00 EUR
✅ Created YNAB transaction for Finolog TX 12345
✅ Finolog sync completed for account 161752: Created 3, Skipped 2, Errors 0
```

### Особенности:

✅ **Только с 1 января 2026** - более ранние транзакции не синхронизируются
✅ **Нет дубликатов** - каждая транзакция создается один раз благодаря import_id
✅ **Односторонняя** - транзакции из YNAB НЕ синхронизируются обратно в Finolog
✅ **Автоматическая конвертация** - RUB и USD конвертируются в EUR

### Проверка результата:

Откройте YNAB → Alex Personal Budget → Account for Epic Web3

Вы увидите новые транзакции с memo:
- `[Description] | Finolog Sync`
- Если description пустое: `Finolog Sync`

## Продакшн

Для непрерывной работы:
```bash
npm start
```

Синхронизация будет происходить каждые 5 минут (настраивается в `.env`).

## Troubleshooting

### "Finolog sync disabled - no API token"
→ Проверьте `.env`: должно быть `FINOLOG_API_TOKEN=...`

### "EUR/RUB exchange rate not found"
→ Добавьте курс для нужного месяца в таблицу `exchange_rates`

### "Cannot convert currency"
→ Отсутствует курс валют для месяца транзакции

### Транзакции не создаются
→ Проверьте дату: синхронизация работает только с 2026-01-01

## Документация

Полная документация: `FINOLOG_SETUP.md`





